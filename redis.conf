# Durable Redis settings for LangGraph checkpoints and the ticket cache.
appendonly yes
appendfsync everysec

# Keep periodic RDB snapshots as an additional recovery option.
save 3600 1
save 300 100
save 60 10000
